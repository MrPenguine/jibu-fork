-- CreateEnum
CREATE TYPE "WorkflowType" AS ENUM ('MASTER', 'SECONDARY');

-- CreateTable
CREATE TABLE "Workflow" (
  "id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "workflowType" "WorkflowType" NOT NULL DEFAULT 'MASTER',
  "nodes" JSONB NOT NULL DEFAULT '{}',
  "edges" JSONB NOT NULL DEFAULT '{}',
  "startNodeId" TEXT,
  "agentId" TEXT NOT NULL,
  "masterWorkflowId" TEXT,
  "isPublished" BOOLEAN NOT NULL DEFAULT false,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL,
  "organizationId" TEXT NOT NULL,
  
  CONSTRAINT "Workflow_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "Workflow_agentId_idx" ON "Workflow"("agentId");
CREATE INDEX "Workflow_masterWorkflowId_idx" ON "Workflow"("masterWorkflowId");
CREATE INDEX "Workflow_workflowType_idx" ON "Workflow"("workflowType");
CREATE INDEX "Workflow_organizationId_idx" ON "Workflow"("organizationId");

-- AddForeignKey
ALTER TABLE "Workflow" ADD CONSTRAINT "Workflow_agentId_fkey" FOREIGN KEY ("agentId") REFERENCES "Agent"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "Workflow" ADD CONSTRAINT "Workflow_masterWorkflowId_fkey" FOREIGN KEY ("masterWorkflowId") REFERENCES "Workflow"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "Workflow" ADD CONSTRAINT "Workflow_organizationId_fkey" FOREIGN KEY ("organizationId") REFERENCES "Organization"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- Data Migration Script (needs to be run separately)
-- This will migrate workflow data from Agent to the new Workflow table
-- INSERT INTO "Workflow" ("id", "name", "description", "nodes", "edges", "startNodeId", "agentId", "isPublished", "organizationId")
-- SELECT uuid_generate_v4(), name, description, nodes, edges, "startNodeId", id, "isPublished", "organizationId" FROM "Agent";
